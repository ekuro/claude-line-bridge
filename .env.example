# LINE Messaging API設定
LINE_CHANNEL_TOKEN=your_channel_token_here
LINE_USER_ID=your_user_id_here

# Claude Code実行ディレクトリ（オプション）
# デフォルト: カレントディレクトリ
CLAUDE_WORKING_DIR=/path/to/your/working/directory

# Claude Codeコマンドパス（オプション）
# デフォルト: claude
# 例: /usr/local/bin/claude, /opt/homebrew/bin/claude
# nvmを使用している場合は which claude で確認してフルパスを指定
CLAUDE_PATH=claude

# Claude Code許可ツール設定（オプション）
# デフォルト: Read,Grep,Glob,LS（環境変数未設定時）
# 空文字列を設定するとすべてのツールで許可が必要
# カンマ区切りで指定（例: Read,Edit,Write,Grep,Glob,LS,Bash）
CLAUDE_ALLOWED_TOOLS=Read,Edit,Write,Grep,Glob,LS

# パーミッションスキップ設定（オプション）
# true: すべてのツール使用を自動許可（--dangerously-skip-permissions）
# false: 対話的にツール使用を許可（デフォルト）
CLAUDE_SKIP_PERMISSIONS=false

# サーバー設定（オプション）
PORT=3000